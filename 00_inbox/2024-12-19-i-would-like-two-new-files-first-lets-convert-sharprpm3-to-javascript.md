---
title: "I would like two new files. first lets convert sharprpm3 to javascript…"
date: 2024-12-19
source-folder: Notes
tags: [javascript, firebase]
---

# I would like two new files. first, let's convert sharprpm3 to javascript…

I would like two new files:

1. First, convert `sharprpm3` to JavaScript and call the file `sharprpm.js`.
2. Next, update the `AddReading` file.

## Updates to AddReading file

- I would like `sharprpm` to provide an output of numbers.
- Format the output to match the `bloodPressureMonitor_ButtonClicked` output.
- Add a new button next to `bloodPressureMonitor_ButtonClicked`. Call it `camera_ButtonClicked`.
- Configure the output of `camera_ButtonClicked` to be identical to `bloodPressureMonitor_ButtonClicked`.
- Submit the output as a reading to Firebase, just like `bloodPressureMonitor_ButtonClicked`.
---
